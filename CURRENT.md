# CURRENT

Items in progress or pending verification before the next release.

## 1. Local + Hybrid Mode Testing
**Status**: TESTING

All features implemented, waiting for manual verification in both modes:
- Local mode: all features unrestricted, no auth
- Hybrid mode: auth, rate limits, path traversal restrictions, egress controls
- Key test areas: notebook export, deploy page, batch predict, folder watch, report export
- 58 mode-matrix tests passing, gateway API key test passing
