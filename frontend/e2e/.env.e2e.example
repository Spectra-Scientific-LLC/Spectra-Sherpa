# Copy to .env.e2e and adjust the target URL when running against a
# non-default dev server. OSS local-mode e2e does not use managed-login
# credentials; /login and /register are supplied only by server modules.
E2E_BASE_URL=http://localhost:5173
