# Production environment variables
# These are baked into the build at compile time

# Use relative URLs - nginx proxies /api to backend
# Leave empty to use the smart defaults in the code
VITE_API_BASE_URL=
